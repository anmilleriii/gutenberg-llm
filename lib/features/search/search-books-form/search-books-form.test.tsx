describe("<SearchBooksForm />", () => {
  it("allows searching books", () => {
    expect(true).toBe(true);
  });

  it("shows missing on missing book id", () => {
    expect(true).toBe(true);
  });

  it("shows success on submit", () => {
    expect(true).toBe(true);
  });

  it("shows no results on no results", () => {
    expect(true).toBe(true);
  });
});
